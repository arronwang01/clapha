# Supercell's game library -- proprietary, never redistribute
*.so
libg-*

# upstream reference clone (its own repo)
ref-cr-native-sandbox/

# build outputs, logs, deck exchange files, match recordings
build/
artifacts/

# game screenshots
screen*.png

# python / macOS
__pycache__/
*.pyc
.DS_Store

# the native app is built from app/ (app/build.sh)
Clapha.app/

# game files pulled from the user's own install (never committed)
runtime/

# upstream FirstLight_CR clone (models + code), kept current by update_check
ref-firstlight/
